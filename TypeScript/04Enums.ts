enum Complete {
    success = 'SUCCESS',
    Error = 'ERROR'
}
let isComplete: Complete = Complete.success
console.log(isComplete);

enum Directions {
    up, down, left, right
}
let dir: Directions = Directions.down
console.log(dir);
